namespace PracticalTask1.Algorithms
{
    public enum TypeSearch
    {
        Equal = 0, // Значения полностью равны
        InPresent = 1, // Присутсвует в поиске. Пример: "норм" присутсвует в "нормальный"
        Missing = 2  // Отсутсвие в поиске
    }
}